{
  "name": "landing-page-node-hosting",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for Landing Page Node Hosting (multi-server)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.67.0"
  }
}
